-- Angel Digital 11+ — Migration 119
-- Mathematics First Mock Structural Capacity, Authoring Increment 001 —
-- Interdependent Algebraic System (Decision 177/178).
--
-- ============================================================
-- WHY THIS MIGRATION EXISTS
-- ============================================================
-- Decision 177's own read-only stress-test found State B/C's real
-- form-composition ceiling (~43-46 marks in an authentic 20-21-question
-- form, ~14-17 marks short of ~58-60) is a pool-composition problem
-- caused by near-total absence of genuine Classification A (shared-
-- system) experiences, and named the interdependent-algebraic-system
-- archetype as the single highest-frequency real structural form not yet
-- represented in Angel's pool. This migration authors exactly ONE such
-- family — the first of several such rounds Decision 177 itself
-- disclosed will be required, not a claim that the deficit is closed.
--
-- ============================================================
-- PRIMARY-SOURCE EVIDENCE LOCK (Part 1 of this session's own directive)
-- ============================================================
-- Read directly this session via pdftotext against the real PDFs in
-- knowledge/csse/official-papers/ and knowledge/csse/mark-schemes/ (not
-- Decision prose). All three mark schemes state, identically: "1 mark
-- for each correct answer. Total 60 marks." Five confirmed instances of
-- the archetype (a shared system of stated numeric relationships, from
-- which 2-3 independently marked unknowns/derived values must be found):
--
--   1. 2023 Q8 — three simultaneous symbol-equations (three unknowns
--      solved from a stated linear system), 3 marks, 1 each (mark
--      scheme confirms three distinct correct-value entries).
--   2. 2023 Q18 — four related quantities defined by stated algebraic
--      rules, three derived-expression sub-questions, 3 marks, 1 each
--      (mark scheme confirms three distinct correct-value entries).
--   3. 2022 Q6 — two multiplicative relations plus one stated sum, two
--      unknowns to find, 2 marks, 1 each (universal "1 mark per answer"
--      rule applied to the paper's own two answer blanks).
--   4. 2021 Q7 — two multiplicative relations plus one stated sum, two
--      unknowns to find, 2 marks, 1 each (same rule, two answer blanks).
--   5. 2021 Q20 — three combined-quantity statements (a real-world
--      three-way system), three unknowns to find, 3 marks, 1 each (mark
--      scheme's own page-level box confirms 3 marks for this question).
--
-- All five verified directly against the real paper text and, where the
-- specific mark scheme entry could be isolated (2023 Q8/Q18, 2021 Q20),
-- against the real mark scheme's own answer key. No claimed instance was
-- found unverifiable; nothing here is taken on Decision prose alone.
--
-- STRUCTURAL ARCHETYPE PROOF, common to all five: (a) a system of
-- numeric relationships between several named quantities is stated
-- ONCE, never restated as new information in a later subpart; (b) each
-- subpart asks for a specific unknown or derived value, genuinely
-- requiring engagement with the stated system (never solvable from a
-- subpart's own text in isolation); (c) later subparts commonly build on
-- an earlier subpart's own found value, in the natural solving order,
-- but the *shared system itself* is what is reused across subparts, not
-- a restated intermediate answer; (d) exactly 1 mark per independently
-- correct value, no partial credit, no single answer worth more than
-- 1 mark; (e) no diagram, image, or chart is used by any of the five —
-- purely textual/numeric. This is the same structural shape Angel's own
-- existing mock-mr06-multiplerelation family already represents at
-- 2-unknown scale (ratio/multiple relation, solve for both values) —
-- this migration extends that SAME QT-MR-06 reasoning skill to a
-- 3-unknown, 3-subpart, genuinely richer system, rather than inventing a
-- new Question Type.
--
-- ============================================================
-- CONTENT AUTHORED: mock-mr06-linkedvalues (QT-MR-06, one numbered-
-- question experience, 3 subparts, using migration 093's grouping
-- columns exactly as migration 109/113 already proved safe)
-- ============================================================
-- Original Angel scenario: three bags of marbles (red/blue/green) with a
-- stated additive relation (blue = red + 6) and a stated multiplicative
-- relation (green = 3 x blue), plus one stated total (64). Not a
-- paraphrase, rewording, or number-substitution of any of the five
-- primary-source instances above: no lettered A/B/C/D system, no box
-- symbols, no age system, no money/purchase system, no rope/tank
-- scenario (already used by mock-mr06-multiplerelation). Every number,
-- name, and context is Angel-original.
--
-- Every answer independently re-derived and hand-verified before this
-- file was written, using two independent methods (direct algebraic
-- solution, and substitution back into every stated relationship):
--   mock-mr06-linkedvalues-01 (a): let red = r; blue = r+6; green =
--     3(r+6); r + (r+6) + 3(r+6) = 64 => 5r + 24 = 64 => 5r = 40 =>
--     r = 8. Check: 8 + 14 + 42 = 64. CORRECT, unique.
--   mock-mr06-linkedvalues-02 (b): blue = 8+6 = 14; green = 3x14 = 42.
--     Check: 8 + 14 + 42 = 64. CORRECT, unique.
--   mock-mr06-linkedvalues-03 (c): red+blue = 8+14 = 22; green-22 =
--     42-22 = 20. Check: 42 = 22+20. CORRECT, unique.
-- Every subpart's answer is a single non-negative integer with exactly
-- one correct value; no alternative interpretation of "how many marbles"
-- or "how many more" produces a second valid answer.
--
-- Subpart progression (Part 3/5 of this session's own directive): (a)
-- accessible entry — set up and solve the one linear equation the
-- stated system implies for the base unknown; (b) relationship
-- reasoning — apply the SAME stated relationships forward from (a)'s
-- own found value to derive a different bag's count; (c) deeper
-- inference — combine all three derived values in a new operation
-- (combined total, then a difference) that neither (a) nor (b) required.
-- Not three unrelated calculations: every subpart depends on the one
-- shared system stated once in each subpart's own restated question
-- text (matching the established restatement convention already used by
-- mock-mr10-fairprep/mock-mr09-runningclub, migration 113).
--
-- ============================================================
-- QT / SKILL CLASSIFICATION (Part 5/10 of this session's own directive)
-- ============================================================
-- QT-MR-06 (Multiple-Relation reasoning) on all 3 rows — the SAME
-- existing Question Type mock-mr06-multiplerelation already uses, not a
-- new QT. This family is a genuine, evidence-backed extension of that
-- same reasoning skill: multiplerelation solves a 2-unknown system
-- (one multiplicative relation, one sum); mock-mr06-linkedvalues solves
-- a 3-unknown system (one additive relation, one multiplicative
-- relation, one sum), matching the real archetype's own typical scale
-- (2-3 unknowns per system, per the five verified instances above).
-- prompt.skill = "algebra" on all 3 rows, matching mock-mr06-
-- multiplerelation's own convention exactly.
--
-- ============================================================
-- MARKS CONTRACT (Decision 175, binding)
-- ============================================================
-- 1 mark per subpart, 3 rows, 3 marks total. No row's marks value
-- exceeds 1. No partial-credit mechanism invoked or implied.
--
-- ============================================================
-- DUPLICATE/OVERLAP AUDIT PERFORMED BEFORE AUTHORING (Part 6 of this
-- session's own directive) — checked against all 194 Mathematics
-- Practice rows, all 48 current mock_eligible rows, all 8 independently_
-- validated rows, and this migration's own 3 rows against each other
-- ============================================================
-- 1. EXACT DUPLICATION: none — repository-wide search for "marble" and
--    "collector" found zero prior use anywhere in this repository.
-- 2. TEXTUAL NEAR-DUPLICATION: none.
-- 3. SUPERFICIAL NUMBER SUBSTITUTION: none — this is a single family,
--    3 subparts, not multiple instances of one shape.
-- 4. EFFECTIVELY IDENTICAL REASONING — the one genuine near-neighbour is
--    mock-mr06-multiplerelation (migration 091, live mock_eligible,
--    QT-MR-06), disclosed honestly, not hidden: multiplerelation is a
--    2-unknown, single-subpart-pair, "one relation + one sum" system
--    (rope/tank scenarios); this family is a 3-unknown, 3-subpart,
--    "two relations + one sum, plus a combined-comparison step" system
--    (marbles scenario) — a structurally richer instance of the SAME
--    reasoning skill, not a relabelling. mock-mr06-sumdiff (migration
--    091) was also checked: its own scenario is abstract (no named
--    objects), 2-unknown sum-and-difference only, no multiplicative
--    relation — structurally distinct.
-- 5. LEGITIMATE STRUCTURAL RECURRENCE: not applicable — only one
--    instance of this family is authored in this migration, per
--    explicit instruction (do not author more than one family, do not
--    create a second instance of this archetype yet).
--
-- ============================================================
-- WHAT THIS MIGRATION DOES NOT DO
-- ============================================================
-- Does not touch any existing row, family, or migration (088-118). Does
-- not alter any of the 48 existing mock_eligible rows, the 8
-- independently_validated reserve rows, or any grouping metadata on
-- them. Does not set eligibility_status to anything other than
-- 'authentic_assessment_candidate'. Does not create or touch any
-- ali_family_review row (migration 120 handles the pending-review
-- placeholder separately). Does not create, modify, or activate any
-- ali_mock_form row. Does not touch English or Writing content. Does
-- not author the shared-timetable family or any other archetype named
-- in Decision 177. Does not reproduce any CSSE past-paper question
-- wording, image, or exact numeric scenario.
--
-- KNOWN LIMITATIONS, disclosed honestly: this single family (1 numbered-
-- question experience, 3 marks) is one bounded step, not a claim the
-- ~14-17-mark deficit found in Decision 177 is closed — Decision 177's
-- own estimate (5-9 further rich families needed) is unchanged by this
-- migration alone. The shared-timetable archetype remains the next
-- likely candidate but is explicitly NOT authored here.
--
-- NOT APPLIED. Generated for independent-reviewer and Founder inspection
-- via Supabase Dashboard > SQL Editor > New query, after migration 118
-- (NOT yet confirmed applied at time of writing — see Decision 178's own
-- reconciliation) has already been applied. This migration does NOT
-- itself grant any review approval — see migration 120 for the
-- pending-review placeholder record.

begin;

insert into public.ali_question_bank
  (id, subject, skill, pathway, content_difficulty, question_type, estimated_time_seconds,
   prompt, explanation, mastery_threshold, learning_unit_id,
   family_id, provenance, eligibility_status, content_version, active, addresses_misconception,
   transfer_class, question_group_id, group_order, subpart_label, marking_mode)
values
('mock-mr06-linkedvalues-01', 'maths', 'QT-MR-06', array['csse'], 'medium', 'short-answer', 90,
 $json${"id":"mock-mr06-linkedvalues-01","marks":1,"skill":"algebra","answer":"8","question":"A collector has three bags of marbles: red, blue and green. The blue bag has 6 more marbles than the red bag. The green bag has 3 times as many marbles as the blue bag. Altogether, the three bags contain 64 marbles. How many marbles are in the red bag?","workingSteps":["Let the red bag = r marbles","Blue = r + 6, Green = 3 x (r + 6)","r + (r + 6) + 3(r + 6) = 64","5r + 24 = 64, so 5r = 40","r = 8"]}$json$,
 'Mathematics First Mock Structural Capacity, Authoring Increment 001 (Decision 177/178). Subpart (a) — QT-MR-06 (Multiple-Relation reasoning), competency MR-06, family mock-mr06-linkedvalues. Genuine interdependent algebraic system: one stated additive relation (blue = red + 6), one stated multiplicative relation (green = 3 x blue), and one stated total (64), matching the real, directly-evidenced structure of 2023 Q8/Q18, 2022 Q6, 2021 Q7/Q20 (all five independently re-verified this session against the real papers and mark schemes, not merely prior Decision prose) — never a paraphrase of any of those scenarios. Answer independently recomputed via two methods (direct solution and back-substitution): r=8, blue=14, green=42, sum=64.', 2, 'mock-mr06-linkedvalues-01',
 'mock-mr06-linkedvalues', 'angel_original', 'authentic_assessment_candidate', 1, true,
 'Treating the blue and green bags as if they were equal to the red bag (ignoring the stated "6 more" and "3 times" relationships), or combining only two of the three bags before solving for the total.',
 'FAR_TRANSFER', 'mock-mr06-linkedvalues', 1, '(a)', 'deterministic'),

('mock-mr06-linkedvalues-02', 'maths', 'QT-MR-06', array['csse'], 'medium', 'short-answer', 75,
 $json${"id":"mock-mr06-linkedvalues-02","marks":1,"skill":"algebra","answer":"42","question":"A collector has three bags of marbles: red, blue and green. The blue bag has 6 more marbles than the red bag. The green bag has 3 times as many marbles as the blue bag. Altogether, the three bags contain 64 marbles. How many marbles are in the green bag?","workingSteps":["The red bag has 8 marbles","Blue = 8 + 6 = 14","Green = 3 x 14 = 42"]}$json$,
 'Mathematics First Mock Structural Capacity, Authoring Increment 001 (Decision 177/178). Subpart (b) — QT-MR-06, family mock-mr06-linkedvalues. Forms one displayed numbered question together with subpart (a) above (same question_group_id), reusing the SAME shared system stated in (a) (not a restated intermediate answer), building forward from the red-bag value to the green bag via both stated relationships in turn — a genuinely different reasoning step from (a) (forward application, not equation-solving). Answer independently recomputed via two methods: blue=8+6=14, green=3x14=42; check 8+14+42=64.', 2, 'mock-mr06-linkedvalues-02',
 'mock-mr06-linkedvalues', 'angel_original', 'authentic_assessment_candidate', 1, true,
 'Multiplying the red bag''s count by 3 directly instead of first adding 6 to find the blue bag, then multiplying the blue bag''s count by 3.',
 'FAR_TRANSFER', 'mock-mr06-linkedvalues', 2, '(b)', 'deterministic'),

('mock-mr06-linkedvalues-03', 'maths', 'QT-MR-06', array['csse'], 'hard', 'short-answer', 90,
 $json${"id":"mock-mr06-linkedvalues-03","marks":1,"skill":"algebra","answer":"20","question":"A collector has three bags of marbles: red, blue and green. The blue bag has 6 more marbles than the red bag. The green bag has 3 times as many marbles as the blue bag. Altogether, the three bags contain 64 marbles. How many more marbles are in the green bag than in the red and blue bags combined?","workingSteps":["Red and blue combined = 8 + 14 = 22","Green = 42","Difference = 42 - 22 = 20"]}$json$,
 'Mathematics First Mock Structural Capacity, Authoring Increment 001 (Decision 177/178). Subpart (c) — QT-MR-06, family mock-mr06-linkedvalues. Deepest reasoning demand of the three: requires all three derived values (red, blue, green) held together and combined in a NEW operation (a combined total, then a difference) that neither (a) nor (b) required — matching the real archetype''s own pattern of an increasingly demanding final subpart (e.g. 2023 Q18(iii)''s own multi-value combined expression). Answer independently recomputed via two methods: (8+14)=22, 42-22=20; check 22+20=42.', 3, 'mock-mr06-linkedvalues-03',
 'mock-mr06-linkedvalues', 'angel_original', 'authentic_assessment_candidate', 1, true,
 'Subtracting only the red bag from the green bag (forgetting to include the blue bag in the combined total), or adding all three bags together instead of finding the difference.',
 'FAR_TRANSFER', 'mock-mr06-linkedvalues', 3, '(c)', 'deterministic')
on conflict (id) do nothing;

commit;
