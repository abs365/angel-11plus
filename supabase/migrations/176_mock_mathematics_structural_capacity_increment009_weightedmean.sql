-- Angel Digital 11+ — Migration 176
-- Mathematics Structural Capacity, Authoring Increment 009 —
-- Two-Group Weighted Mean and its Algebraic Reverse (Founder Completion
-- and Readiness Programme, continuation directive, 2026-09-01: the fifth
-- and final Decision 226 gap archetype).
--
-- ============================================================
-- CITATION CORRECTION (disclosed, not silently fixed)
-- ============================================================
-- Migration 174 flagged that Decision 226 Section 7's citation for this
-- archetype ("weighted-differential-rate mean extension | 2022 Q15") does
-- not match the real 2022 Q15 text (independently re-read that session:
-- Q15 is a museum-visitor bar-chart mean question with no weighted-
-- group-combination structure at all) — a citation-fidelity gap, not
-- authored around with a guessed reinterpretation.
--
-- Direct re-reading of BOTH 2023-Entry primary-source documents this
-- session (already read in full for migration 174) found the actual
-- source for this archetype: **2023 Q19(b)/(c)**, not 2022 Q15. Q19(b):
-- "five boys averaged a score of 7 and ten girls averaged a score of 10.
-- Find the average (mean) for all fifteen children" (mark scheme answer
-- 9 — independently re-derived this session: 5x7=35, 10x10=100,
-- (35+100)/15=9). Q19(c): "A different test was sat by two boys and a
-- certain number of girls. The children achieved an average score of 9.
-- The average for the two boys was 6 and the average for the girls was
-- 10. How many children were there in all?" (mark scheme answer 8 —
-- independently re-derived: let girls=g, (12+10g)/(2+g)=9 => g=6, total
-- =8). This is exactly the "genuine evidenced extension of the existing
-- frequency-table-mean shape" Decision 226 described — combining two
-- differently-sized, differently-averaged groups into one mean, and its
-- algebraic reverse (finding an unknown group size from a stated
-- combined mean) — just cited to the wrong question number. Reported
-- here as a correction for the Founder's awareness, per this project's
-- own standing citation-fidelity disclosure practice (e.g. migration
-- 140's note on mock-mr03mr07-perimeterarea).
--
-- Q19(a) (plain mean of a six-number list) is NOT authored here — it is
-- not the genuinely new archetype; Angel already has substantial plain-
-- mean coverage (mr01-mean-01..04, MR-01 "Adequate"). Only the two-group
-- weighted-combination structure (b) and its reverse (c) are new.
--
-- ============================================================
-- ESTATE OVERLAP AUDIT — LOW OVERLAP, proceeding
-- ============================================================
-- The only existing mock-track MR-12/mean-related family is
-- `mock-mr12-reversemean` (migrations 081/083, Practice `mr01-revmean-
-- 01..04`, live-content-verified this session by direct reading of
-- migration 081): finds a SINGLE missing value from ONE group's known
-- mean (e.g. "4 of 5 scores + mean given, find the 5th score"). This is
-- a genuinely different reasoning skill from combining TWO differently-
-- sized groups' means into one, or reversing that combination to find an
-- unknown group SIZE (not a missing individual value). OVERLAP
-- CLASSIFICATION: LOW.
--
-- ============================================================
-- AUTHORING GATE — ALL SEVEN CONDITIONS MET
-- ============================================================
-- 1. SOURCE-CONTAINS above (corrected citation, independently verified
-- against the real paper and mark scheme). 2. Materially underrepresented
-- (overlap audit above). 3. Text-only, no diagram/table required.
-- 4. KS2 Year 6 content (mean/average, core Number curriculum), same
-- plain language the source itself uses. 5. Two coherent, independent
-- numbered experiences (not grouped into one question_group_id, mirroring
-- the source's own "a DIFFERENT test..." scenario break between (b) and
-- (c) — the same disclosed pattern migration 174's pyramid family used).
-- 6. Genuine new structural capacity: two-group weighted combination and
-- its algebraic reverse, not a renamed reversemean. 7. Every reasoning
-- demand traces to source evidence (each row's own explanation).
--
-- ============================================================
-- TWO FAMILIES (deliberately separate, not one grouped family — see
-- below): mock-mr12-weightedmeancombine and mock-mr12-weightedmeanreverse
-- (QT-MR-12, reused — "Average (Mean) Calculation", MR-01, matching 2023
-- Q19's own primary competency)
-- ============================================================
-- AUTHORED-EXTRAPOLATION: an original two-class quiz-score scenario
-- (Class 5A / Class 5B) — not a reproduction of the source's boys/girls
-- framing or numbers. Row -01 (4 pupils mean 6, 6 pupils mean 11,
-- combined mean of 10) and row -02 (3 pupils mean 8, unknown-size group
-- mean 12, combined mean 10, find the unknown group size) are
-- deliberately given DIFFERENT numbers from each other as well as from
-- the source, avoiding the kind of accidental unnecessary numeric
-- coincidence this project's own review culture would flag.
--
-- TWO-FAMILY DESIGN CHOICE (disclosed): unlike every grouped family
-- authored earlier this session (roundingbounds-style subparts of ONE
-- shared scenario), these two rows are genuinely independent scenarios —
-- "a different quiz" with different pupils and numbers, mirroring the
-- source's own scenario break between 19(b) and 19(c) — not subparts of
-- a single numbered experience. Per Decision 226's own family-counting
-- convention (each distinct scenario/reasoning-thread is its own family
-- for anti-memorisation and capacity-register purposes), this migration
-- therefore authors TWO single-row families rather than one artificially
-- grouped family, so the capacity register counts them honestly as 2
-- families / 2 numbered experiences, not 1. No `sharedStem` or
-- `question_group_id` is used for either row.
--
-- MATHEMATICAL VERIFICATION (two methods each):
--   (-01) Method 1 (weighted-total formula): Class 5A total = 4x6=24;
--       Class 5B total = 6x11=66; combined total=90, combined count=10,
--       combined mean=9. Method 2 (direct listing of individual
--       reasoning): the class-5A group contributes 24 "score-units"
--       across 4 pupils and class-5B contributes 66 "score-units" across
--       6 pupils; sharing all 90 score-units evenly across all 10 pupils
--       gives 9 each. Both agree: 9.
--   (-02) Method 1 (algebraic): (24+12g)/(3+g)=10 => 24+12g=30+10g =>
--       2g=6 => g=3. Method 2 (direct check): 3 pupils at 8 = 24; 3
--       pupils at 12 = 36; combined total=60, combined count=6, combined
--       mean=60/6=10, matching the stated combined mean exactly. Both
--       agree: 3.
--
-- SUBPART INDEPENDENCE: both rows are fully self-contained, independent
-- numbered experiences (not subparts of one shared scenario), so this is
-- structurally moot but noted for completeness — matches this family's
-- own explicit two-scenario design.
--
-- ANTI-MEMORISATION: no existing Angel family combines two differently-
-- sized, differently-averaged groups into one weighted mean, or reverses
-- that combination algebraically to find an unknown group size —
-- genuinely distinct from mock-mr12-reversemean's single-missing-value
-- structure.
--
-- ============================================================
-- WHAT THIS MIGRATION DOES NOT DO
-- ============================================================
-- Does not touch any existing row, family, or migration (088-175). Does
-- not alter any existing mock_eligible/independently_validated row,
-- including mock-mr12-reversemean. Does not set eligibility_status to
-- anything other than 'authentic_assessment_candidate'. Does not create
-- or touch any ali_family_review row (migration 177 handles the
-- pending-review placeholder separately). Does not create, modify, or
-- activate any ali_mock_form row. Does not touch English or Writing
-- content. Does not author 2023 Q19(a) (plain mean of a list — not the
-- genuinely new archetype, see above). Does not build or imply any
-- diagram/geometry rendering capability. Does not reproduce any CSSE
-- past-paper question wording, image, or exact numeric scenario.
--
-- FAIL-CLOSED / DUPLICATE-ID PROTECTION: `on conflict (id) do nothing`,
-- matching every prior content migration in this repository exactly.
--
-- NOT APPLIED. Generated for Founder review and manual application via
-- Supabase Dashboard > SQL Editor > New query. With this migration, all
-- five of Decision 226 Section 7's confirmed Mathematics gap archetypes
-- have been authored this session (migrations 170, 174, 176), each
-- independently primary-source-verified rather than trusted from
-- Decision 226's own summary.

begin;

insert into public.ali_question_bank
  (id, subject, skill, pathway, content_difficulty, question_type, estimated_time_seconds,
   prompt, explanation, mastery_threshold, learning_unit_id,
   family_id, provenance, eligibility_status, content_version, active, addresses_misconception,
   transfer_class, question_group_id, group_order, subpart_label, marking_mode)
values

('mock-mr12-weightedmean-01', 'maths', 'QT-MR-12', array['csse'], 'medium', 'short-answer', 70,
 $json${"id":"mock-mr12-weightedmean-01","marks":1,"skill":"number-properties","answer":"9","question":"Class 5A has 4 pupils with a mean quiz score of 6. Class 5B has 6 pupils with a mean quiz score of 11. What is the mean quiz score for all 10 pupils in the two classes combined?","workingSteps":["Class 5A's total score = 4 x 6 = 24","Class 5B's total score = 6 x 11 = 66","Combined total score = 24 + 66 = 90, combined number of pupils = 4 + 6 = 10","Combined mean = 90 / 10 = 9"]}$json$,
 'Mathematics Structural Capacity, Authoring Increment 009 (Founder Completion and Readiness Programme, 2026-09-01). QT-MR-12 (Average/Mean Calculation), family mock-mr12-weightedmeancombine. SOURCE-CONTAINS: structural analogue of 2023 Q19(b) (independently re-verified this session against the real paper and mark scheme: five boys mean 7, ten girls mean 10, combined mean 9) -- see this migration''s own header for the citation correction from Decision 226''s original "2022 Q15" mis-citation. Answer independently recomputed via two methods (weighted-total formula; direct score-unit-sharing reasoning): 9.', 2, 'mock-mr12-weightedmean-01',
 'mock-mr12-weightedmeancombine', 'angel_original', 'authentic_assessment_candidate', 1, true,
 'Finding the mean of the two GROUP means directly ((6+11)/2 = 8.5) instead of weighting each class''s mean by its own number of pupils -- this only works when both groups are the same size, which they are not here.',
 'FAR_TRANSFER', null, null, null, 'deterministic'),

('mock-mr12-weightedmean-02', 'maths', 'QT-MR-12', array['csse'], 'hard', 'short-answer', 100,
 $json${"id":"mock-mr12-weightedmean-02","marks":1,"skill":"number-properties","answer":"3","question":"A different quiz was sat by 3 pupils from Class 5A and a number of pupils from Class 5B. The 3 pupils from Class 5A had a mean score of 8. The pupils from Class 5B had a mean score of 12. The combined mean score for everyone who sat this quiz was 10. How many pupils from Class 5B sat this quiz?","workingSteps":["Let the number of Class 5B pupils be g","Class 5A's total score = 3 x 8 = 24. Class 5B's total score = 12g","Combined total divided by combined count equals the combined mean: (24 + 12g) / (3 + g) = 10","24 + 12g = 10 x (3 + g) = 30 + 10g, so 2g = 6, g = 3"]}$json$,
 'Mathematics Structural Capacity, Authoring Increment 009 (Founder Completion and Readiness Programme, 2026-09-01). QT-MR-12, family mock-mr12-weightedmeanreverse. A different quiz from row -01, fully self-contained, mirroring the real source''s own scenario shift between 19(b) and 19(c). SOURCE-CONTAINS: structural analogue of 2023 Q19(c) (independently re-verified: two boys mean 6, girls mean 10, combined mean 9, answer 8 total children). Answer independently recomputed via two methods (algebraic solve; direct check): 3.', 2, 'mock-mr12-weightedmean-02',
 'mock-mr12-weightedmeanreverse', 'angel_original', 'authentic_assessment_candidate', 1, true,
 'Setting up the equation using the WRONG total pupil count (e.g. forgetting to add the 3 Class 5A pupils into the denominator, or forgetting the +g inside the total), or solving (24+12g)/(3+g)=10 by cross-multiplying incorrectly.',
 'FAR_TRANSFER', null, null, null, 'deterministic')

on conflict (id) do nothing;

commit;
