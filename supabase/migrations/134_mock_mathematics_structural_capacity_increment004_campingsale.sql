-- Angel Digital 11+ — Migration 134
-- Mathematics Structural Capacity, Authoring Increment 004 — Percentage/
-- Ratio Multi-Stage Narrative Family (Decision 195/196).
--
-- ============================================================
-- WHY THIS MIGRATION EXISTS
-- ============================================================
-- Decision 195's own recomputed evidence found the dominant, unchanged
-- bottleneck across three consecutive decisions remains an insufficient
-- number of rich, 3-4-mark, genuine Classification-A experiences, with a
-- newly-elevated secondary weakness (only 3 of 27 named Mathematics
-- families carry any non-medium-difficulty row at all). It authorised
-- authoring exactly one new capability-ready Classification-A family:
-- a percentage/ratio multi-stage narrative chain, deliberately a genuine
-- narrative rather than another table (mock-mr09-funrun, migration 131,
-- already substantially exercised the shared-table archetype).
--
-- ============================================================
-- PRIMARY-SOURCE EVIDENCE LOCK (re-read directly this session via
-- pdftotext against the real official papers, not Decision prose)
-- ============================================================
-- 2023 Q4 (CSSE-006, 6 marks, 3 subparts): (a) "Find 15% of 620" --
-- direct percentage-of; (b) a 20% discount applied to a stated per-item
-- price for a bulk purchase -- forward percentage decrease; (c) given
-- the discounted bulk total, find the per-item price -- genuine reverse
-- percentage reasoning.
-- 2022 Q14 (CSSE-011, 6 marks, 2 subparts): a shared coat scenario
-- (original £64, 15% sale discount, 10% loyalty discount, customer
-- expects 25% off combined) -- (a) the expected (additive) price;
-- (b) the actual price when the second discount is applied to the
-- already-discounted price, not the original -- a genuine, real,
-- examined test of the successive-vs-additive-percentage misconception.
-- 2021 Q19 (CSSE-016, 7 marks, 3 subparts): (a) "Find 15% of £140" --
-- direct percentage-of; (b) a stated price increased by 20% -- forward
-- percentage increase; (c) given the increased price, find the original
-- price before the increase -- genuine reverse percentage reasoning.
--
-- Evidence sufficiency verdict: SUFFICIENT for a genuine 4-subpart
-- Classification-A family combining (i) a direct percentage-of/change
-- calculation, (ii) a second sequential percentage change applied to an
-- already-changed value, (iii) the real, examined successive-vs-additive
-- misconception as its own explicit comparison target, and (iv) reverse
-- percentage reasoning -- all four demands independently confirmed
-- recurring across all three available real papers, never reproducing
-- any of their exact wording, numbers, or named items (coat/T-shirt/
-- jacket/book).
--
-- ============================================================
-- FAMILY AUTHORED: mock-mr04-campingsale (QT-MR-04, reused -- Angel's
-- own existing "Percentage/Proportional Change" Question Type, not a
-- new one; 4 rows, 1 numbered experience, 4 marks, TEXT-ONLY shared
-- narrative + sharedStem -- no table/stimulus, per Decision 195 Part 9's
-- preference for a genuine narrative rather than a further table, and
-- following the already-proven text-only sharedStem precedent
-- established by mock-mr06-linkedvalues, migrations 121/122)
-- ============================================================
-- Original scenario (a camping shop selling tents) -- not any of the
-- real papers' own coat/T-shirt/jacket/book/bicycle/watch/console/tablet
-- items, and not any existing Angel family's own item (mr04-cpct-01 uses
-- a "£80 jacket"; this family never combines the words "jacket" and
-- "£80" together, and uses a different item, £120 base price, and
-- narrative entirely). No CSSE wording, names, or numbers reproduced.
--
-- sharedStem used (Decision 180's contract): "A camping shop sells
-- tents." -- deliberately short and generic so that it remains a
-- literal, exact prefix of all 4 rows' own `question` text, including
-- subpart (d), which introduces a SECOND, different tent with its own
-- unknown original price (the reverse-reasoning target) and therefore
-- cannot share the £120 fact the other three subparts do. Verified this
-- session, before writing this file, that the declared stem is an
-- exact, literal prefix of every one of the 4 rows' own `question` text
-- and that every resulting tail is non-empty -- the same rule
-- resolveGroupSharedStem() enforces at render time.
--
-- ============================================================
-- STRUCTURAL DISTINCTION FROM EXISTING NEIGHBOURS (audited before
-- authoring, per this session's own directive)
-- ============================================================
-- mr04-far-percent / mr04-far-recipe (migrations 036/039): standalone,
-- UNGROUPED single-row proportional-scaling items (no shared scenario
-- across rows, no sequential-percentage or reverse-percentage
-- reasoning) -- Classification B/S at best, never a grouped
-- Classification-A experience.
-- mr04-compound-percentage (migration 040): standalone, UNGROUPED
-- single-row successive-percentage-change items (increase then
-- decrease, both applied to the SAME stated original price) -- tests
-- only the forward compound calculation, never the successive-vs-
-- additive misconception as an explicit comparison target, and never
-- reverse percentage reasoning. This family shares no scenario, numbers,
-- or answer values with mr04-compound-percentage's five rows.
-- mock-mr06-linkedvalues: a genuine Classification-A shared-scenario
-- family, but an interdependent ALGEBRAIC system (additive/
-- multiplicative relations between unknowns), not percentage/ratio
-- reasoning -- no overlap in reasoning skill.
-- mock-mr09-funrun / mock-mr10-bustimetable / mock-mr13-craftstall:
-- all genuine Classification-A, but each keyed to a structured TABLE
-- stimulus (data-handling / timetable / price-list), not a text-only
-- narrative -- this family is deliberately the estate's first
-- text-only-narrative Classification-A Mock family since
-- mock-mr06-linkedvalues.
-- No shared scenario, item, numbers, or answer values with any of the
-- above. Repository-wide search for "camping shop"/"campingsale"/
-- "tents" confirms zero prior use anywhere.
--
-- ============================================================
-- REASONING-DIVERSITY PROOF (four genuinely different demands, not four
-- repetitions of one operation)
-- ============================================================
-- (a) direct one-step percentage decrease applied to a stated original
--     price (easy).
-- (b) a SECOND, independent percentage decrease applied to an
--     already-reduced, explicitly-stated price -- a materially
--     different demand from (a): the base for the percentage is not
--     the original price (medium).
-- (c) an explicit COMPARISON between the actual sequential-discount
--     result and a hypothetical single-combined-percentage result --
--     directly targets the real, examined successive-vs-additive
--     percentage misconception (2022 Q14's own structural role) as its
--     own computed quantity, not merely a third discount calculation
--     (hard): cannot be solved by repeating (a) or (b)'s own arithmetic
--     alone: it requires computing a second, independent hypothetical
--     value and then a directional difference between the two.
-- (d) REVERSE percentage reasoning on a wholly independent second tent
--     with its own unstated original price -- requires dividing by the
--     retained-value factor rather than multiplying by it, the
--     genuinely new operation of the four, and the one the current
--     Mathematics estate has no other grouped Classification-A instance
--     of (hard).
--
-- ============================================================
-- ANSWERS INDEPENDENTLY VERIFIED VIA TWO METHODS before this file was
-- written (manual arithmetic and a re-derivation via a different route)
-- ============================================================
--   (a) 15% off £120. Method 1 (discount-then-subtract): 15% of £120 =
--       £120 × 0.15 = £18; £120 − £18 = £102. Method 2 (direct
--       retained-fraction): £120 × 0.85 = £102. Both agree: £102.
--   (b) 10% off the already-reduced £102. Method 1 (discount-then-
--       subtract): 10% of £102 = £10.20; £102 − £10.20 = £91.80. Method
--       2 (direct retained-fraction): £102 × 0.9 = £91.80. Both agree:
--       £91.80.
--   (c) actual sequential price £91.80 versus a single 25% discount off
--       the original £120. Method 1: £120 × 0.75 = £90; £91.80 − £90 =
--       £1.80. Method 2 (combined-multiplier check): the sequential
--       route's combined multiplier is 0.85 × 0.9 = 0.765 (a 23.5%
--       overall reduction), strictly less than the single 25% route's
--       0.75 multiplier (a 25% reduction); £120 × (0.765 − 0.75) =
--       £120 × 0.015 = £1.80. Both methods agree: £1.80, and confirm the
--       direction (sequential discounts are less generous than the
--       equivalent additive discount, so the actual price is genuinely
--       HIGHER by £1.80, never lower or equal -- the question's own "how
--       much MORE" framing is therefore unambiguous, not merely a
--       plausible guess at direction).
--   (d) a single 20% discount reduces a tent to £136; find the original
--       price. Method 1 (division): a 20% discount retains 80% of the
--       original value, so original = £136 ÷ 0.8 = £170. Method 2
--       (forward check): £170 × 0.8 = £136, confirming the reverse
--       computation. Both agree: £170.
--
-- Every answer stored as a bare currency string (never a free-text
-- phrase), keeping every answer deterministically, unambiguously
-- exact-match scorable under the current marking architecture, matching
-- the established convention from migrations 119/121/125/131. All four
-- answers are exact to the penny -- no rounding instruction is required
-- or used anywhere in this family (a deliberate difference from
-- mock-mr09-funrun's own 1-decimal-place rounding contract, adding
-- presentation diversity rather than repeating the same answer-format
-- pattern).
--
-- ============================================================
-- SUBPART INDEPENDENCE PROOF (no subpart's marking depends on a
-- learner's own answer to a previous subpart)
-- ============================================================
-- (a) is fully self-contained: original price and discount percentage
-- are both stated in its own text.
-- (b) restates the necessary intermediate fact directly ("a tent that
-- was reduced by 15% to £102") as a given, not as "your answer to (a)"
-- -- a learner who answered (a) incorrectly can still answer (b)
-- correctly and receive its mark, exactly as mock-mr09-funrun's own
-- subparts (c)/(d) already established this technique.
-- (c) restates both necessary facts directly ("originally priced at
-- £120 actually costs £91.80") as givens, independent of the learner's
-- own working in (a)/(b).
-- (d) is a wholly independent scenario (a different, second tent) with
-- its own complete, self-contained given facts -- no dependency on
-- (a)-(c) of any kind.
--
-- ============================================================
-- DIFFICULTY, MARKING, MARKS CONTRACT
-- ============================================================
-- (a) easy -- a single, direct one-step percentage-of/decrease
-- calculation on a stated original value; deliberately labelled easy,
-- not medium, to help address Decision 195's own disclosed
-- difficulty-tier imbalance (only 3 of 27 named families had any
-- non-medium row before this migration).
-- (b) medium -- a second, independent percentage decrease applied to an
-- already-changed base value.
-- (c) hard -- requires computing an independent hypothetical value and
-- then a directional comparison between two results, the deepest
-- evaluative demand of the four.
-- (d) hard -- reverse percentage reasoning (division by the
-- retained-value factor), a genuinely different operation from (a)-(c).
-- 1 mark per subpart, 4 rows, 4 marks total. No row's marks value
-- exceeds 1. No partial-credit mechanism invoked or implied.
-- marking_mode is 'deterministic' throughout -- every answer is a single
-- exact-match currency string, so no evidence exists that deterministic
-- marking is unsuitable here.
--
-- ============================================================
-- ANTI-MEMORISATION -- FUTURE VARIANT DIMENSIONS (not authored here)
-- ============================================================
-- At least three independent dimensions along which future instances of
-- this archetype could vary without repeating this exact family: (1)
-- the two percentage rates in (a)/(b) (any pair of clean percentages
-- applied in either order); (2) the comparison target in (c) (a single
-- discount vs. two sequential discounts is one instance; a future
-- variant could instead compare two DIFFERENT sequential-discount
-- orderings, since percentage discounts commute in combined multiplier
-- but not in intermediate values, offering a distinct misconception
-- probe); (3) the direction and rate of the reverse-percentage case in
-- (d) (a discount here; a future variant could use a percentage
-- INCREASE reversal instead, as 2021 Q19(c) itself demonstrates with a
-- price increase rather than a decrease).
--
-- ============================================================
-- DIAGRAM/GEOMETRY BOUNDARY PRESERVED
-- ============================================================
-- This family is text-only narrative content; it requires, uses, and
-- implies no diagram, chart, or geometry rendering capability of any
-- kind. The absence of diagram/geometry-rendering capability remains a
-- disclosed, unresolved structural gap (Decision 189/191/195), not
-- something this migration claims to have closed or worked around.
--
-- ============================================================
-- WHAT THIS MIGRATION DOES NOT DO
-- ============================================================
-- Does not touch any existing row, family, or migration (088-133). Does
-- not alter any of the 55 existing mock_eligible rows or any of the 15
-- existing independently_validated reserve rows. Does not set
-- eligibility_status to anything other than 'authentic_assessment_
-- candidate'. Does not create or touch any ali_family_review row
-- (migration 135 handles the pending-review placeholder separately).
-- Does not create, modify, or activate any ali_mock_form row. Does not
-- touch English or Writing content. Does not author a second family.
-- Does not build or imply any diagram/geometry rendering capability.
-- Does not reproduce any CSSE past-paper question wording, image, or
-- exact numeric scenario.
--
-- FAIL-CLOSED / DUPLICATE-ID PROTECTION: the insert uses
-- `on conflict (id) do nothing`, matching every prior content migration
-- in this repository exactly -- if any of these 4 IDs already exist
-- (drift, re-run, or a naming collision with unrelated content), no row
-- is silently overwritten; the migration becomes a safe no-op for that
-- row rather than corrupting existing state.
--
-- NOT APPLIED. Generated for Founder review and manual application via
-- Supabase Dashboard > SQL Editor > New query.

begin;

insert into public.ali_question_bank
  (id, subject, skill, pathway, content_difficulty, question_type, estimated_time_seconds,
   prompt, explanation, mastery_threshold, learning_unit_id,
   family_id, provenance, eligibility_status, content_version, active, addresses_misconception,
   transfer_class, question_group_id, group_order, subpart_label, marking_mode)
values
('mock-mr04-campingsale-01', 'maths', 'QT-MR-04', array['csse'], 'easy', 'short-answer', 70,
 $json${"id":"mock-mr04-campingsale-01","marks":1,"skill":"percentage","answer":"£102","question":"A camping shop sells tents. One tent has an original price of £120. For a summer sale, the shop reduces this tent's price by 15%. What is the tent's price during the summer sale?","workingSteps":["15% of £120 = £120 × 0.15 = £18","Sale price = £120 − £18 = £102"],"sharedStem":"A camping shop sells tents."}$json$,
 'Mathematics Structural Capacity, Authoring Increment 004 (Decision 195/196). Subpart (a) — QT-MR-04 (Percentage/Proportional Change), family mock-mr04-campingsale. Genuine shared percentage narrative, evidenced by 2023 Q4(a)/2021 Q19(a) (both real direct percentage-of calculations opening a multi-part percentage question) and 2022 Q14 (a real shared-discount narrative), independently re-verified this session against the real papers. Direct one-step percentage decrease on the stated original price. Answer independently recomputed via two methods (discount-then-subtract, direct retained-fraction): £120×0.85=£102.', 2, 'mock-mr04-campingsale-01',
 'mock-mr04-campingsale', 'angel_original', 'authentic_assessment_candidate', 1, true,
 'Subtracting 15 (a fixed amount) instead of 15% of £120, or subtracting £18 from the wrong base value.',
 'MIXED_TRANSFER', 'mock-mr04-campingsale', 1, '(a)', 'deterministic'),

('mock-mr04-campingsale-02', 'maths', 'QT-MR-04', array['csse'], 'medium', 'short-answer', 90,
 $json${"id":"mock-mr04-campingsale-02","marks":1,"skill":"percentage","answer":"£91.80","question":"A camping shop sells tents. A tent that was reduced by 15% to £102 is then given a further 10% off for loyalty-card customers. What does the loyalty-card customer pay for the tent?","workingSteps":["10% of £102 = £102 × 0.10 = £10.20","Loyalty-card price = £102 − £10.20 = £91.80"],"sharedStem":"A camping shop sells tents."}$json$,
 'Mathematics Structural Capacity, Authoring Increment 004 (Decision 195/196). Subpart (b) — QT-MR-04, family mock-mr04-campingsale. Reuses the SAME shared tent-sale scenario as subpart (a) but restates the necessary intermediate fact directly (the already-reduced £102 price) rather than depending on the learner''s own answer to (a) — a genuinely different reasoning demand: a second percentage decrease applied to an already-changed base, evidenced by 2022 Q14''s own real sequential-discount structure. Answer independently recomputed via two methods (discount-then-subtract, direct retained-fraction): £102×0.9=£91.80.', 2, 'mock-mr04-campingsale-02',
 'mock-mr04-campingsale', 'angel_original', 'authentic_assessment_candidate', 1, true,
 'Applying the 10% discount to the original £120 price instead of the already-reduced £102 sale price, treating the two discounts as independent of each other rather than sequential.',
 'MIXED_TRANSFER', 'mock-mr04-campingsale', 2, '(b)', 'deterministic'),

('mock-mr04-campingsale-03', 'maths', 'QT-MR-04', array['csse'], 'hard', 'short-answer', 130,
 $json${"id":"mock-mr04-campingsale-03","marks":1,"skill":"percentage","answer":"£1.80","question":"A camping shop sells tents. Some customers think a 15% discount followed by a 10% discount is the same as a single 25% discount. A tent originally priced at £120 actually costs £91.80 after the two discounts are applied in order. How much MORE is this than the price a single 25% discount straight off the £120 original price would give?","workingSteps":["Single 25% discount: £120 × 0.75 = £90","Actual two-discount price = £91.80 (given)","Difference = £91.80 − £90 = £1.80"],"sharedStem":"A camping shop sells tents."}$json$,
 'Mathematics Structural Capacity, Authoring Increment 004 (Decision 195/196). Subpart (c) — QT-MR-04, family mock-mr04-campingsale. Restates both necessary facts directly (the £120 original, the £91.80 actual price) as givens, independent of the learner''s own working in (a)/(b). Directly targets the real, examined successive-vs-additive percentage misconception (2022 Q14''s own structural role) as an explicit computed comparison, not merely a third discount calculation — the deepest evaluative demand of the four. Answer independently recomputed via two methods (direct subtraction, combined-multiplier check: 0.85×0.9=0.765 vs 0.75, difference 0.015×£120=£1.80): £1.80, direction confirmed (sequential discounts are less generous, so the actual price is genuinely higher).', 2, 'mock-mr04-campingsale-03',
 'mock-mr04-campingsale', 'angel_original', 'authentic_assessment_candidate', 1, true,
 'Assuming a 15% discount followed by a 10% discount is equivalent to a single 25% discount, and therefore answering £0 (no difference) instead of computing and comparing both amounts.',
 'MIXED_TRANSFER', 'mock-mr04-campingsale', 3, '(c)', 'deterministic'),

('mock-mr04-campingsale-04', 'maths', 'QT-MR-04', array['csse'], 'hard', 'short-answer', 140,
 $json${"id":"mock-mr04-campingsale-04","marks":1,"skill":"percentage","answer":"£170","question":"A camping shop sells tents. A different tent is sold after a single 20% discount is applied to its own original price, making its sale price £136. What was that tent's original price before the discount?","workingSteps":["A 20% discount means the sale price is 80% of the original price","£136 ÷ 0.8 = £170","Check: £170 × 0.8 = £136"],"sharedStem":"A camping shop sells tents."}$json$,
 'Mathematics Structural Capacity, Authoring Increment 004 (Decision 195/196). Subpart (d) — QT-MR-04, family mock-mr04-campingsale. A wholly independent second tent with its own complete, self-contained given facts — no dependency on (a)-(c) of any kind. Reverse percentage reasoning (division by the retained-value factor rather than multiplication), evidenced by 2023 Q4(c) and 2021 Q19(c) (both real reverse-percentage subparts: given a changed price, find the original), the genuinely new operation of the four and the one the current Mathematics estate has no other grouped Classification-A instance of. Answer independently recomputed via two methods (division, forward-multiplication check): £136÷0.8=£170, confirmed by £170×0.8=£136.', 2, 'mock-mr04-campingsale-04',
 'mock-mr04-campingsale', 'angel_original', 'authentic_assessment_candidate', 1, true,
 'Adding 20% of the £136 sale price back on (£136 + £27.20 = £163.20) instead of recognising that £136 represents 80% of the unknown original price and dividing by 0.8.',
 'MIXED_TRANSFER', 'mock-mr04-campingsale', 4, '(d)', 'deterministic')
on conflict (id) do nothing;

commit;
