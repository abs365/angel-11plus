# Regression and Verification Report

Per Section 23's instruction: only mark PASS where actually performed; NOT VERIFIED where access or tooling prevents genuine verification.

1. **TypeScript clean.** PASS — `npx tsc --noEmit -p .` clean after every change in this increment.
2. **Lint clean.** PASS — `npx eslint` clean on every new/modified file in this increment. Two pre-existing errors unrelated to this increment remain in `app/pathways/page.tsx` and `app/dashboard/page.tsx` (`react-hooks/set-state-in-effect`), confirmed present before this increment by direct diff comparison against the unmodified files (same technique used and disclosed in the prior dash-punctuation round of this programme).
3. **Production build clean.** PASS — `npm run build` clean, all 53 routes generated, repeated numerous times during this increment.
4. **No new console errors.** PASS with one disclosed observation — see `IMPLEMENTATION_REPORT.md`'s "unresolved observation" (a hidden, non-interactive, possibly-prefetch-related duplicate `<main>` found during ablation testing; no console error was ever logged for it, and the visible/interactive tree was repeatedly confirmed correct).
5. **Desktop verified.** PASS — interactive testing performed at desktop viewport throughout.
6. **Mobile verified.** NOT VERIFIED — the mobile drawer's `PathwaySwitcher` placement was implemented and typechecks/builds, but was not interactively exercised at a mobile viewport width in this session due to time constraints. Recommend a follow-up mobile pass before relying on it.
7. **Pathway persistence verified.** PASS (client-side/localStorage level) — confirmed `selectedPathwayId` persists across page reloads and pathway switches. NOT VERIFIED at database level — migration 026 is prepared but not applied (no privileged Supabase access this session); see `PATHWAY_EVIDENCE_INTEGRITY_ASSESSMENT.md`.
8. **Pathway switching verified.** PASS — first-choice (no confirmation), genuine switch (confirmation dialog shown, correct copy, no dash punctuation), and Cancel (pathway unchanged) all directly tested and confirmed correct.
9. **Historical evidence preservation verified.** PASS (client-side level) — seeded `scores`, `completedLessons`, `xp` before a CSSE→ISEB→CSSE round-trip switch; confirmed byte-for-byte unchanged afterward.
10. **Pathway-specific evidence integrity verified.** PASS by construction, documented in full in `PATHWAY_EVIDENCE_INTEGRITY_ASSESSMENT.md` — no database column exists to verify directly (see item 7), but the Learning Engine's CSSE-only gate and the disjoint CSSE/legacy competency-code namespaces are confirmed by direct code inspection to make cross-pathway contamination structurally impossible today.
11. **CSSE legacy-content isolation verified.** PASS — fresh reachability audit performed (`LEGACY_REACHABILITY_AUDIT.md`); found and fixed 5 real routing-leakage sites (`buildItem()`, replay-item mission entry, `buildFocusAreas()`, Dashboard's Continue Learning row, Angel Plus's Journey Links); confirmed no remaining hardcoded legacy href reachable from a CSSE learner's normal journey.
12. **GL regression verified.** PASS — set pathway to `gl`, confirmed top-bar switcher shows "GL", confirmed Learn/Practise links correctly remain `/learn`/`/reasoning` (unchanged, as intended).
13. **CEM regression verified.** NOT VERIFIED directly — CEM shares the exact same code path as GL/ISEB/Independent (the `isCsse` boolean branch is the only pathway-conditional logic touching routing), and GL was directly verified; not separately re-tested due to time constraints, but no code path exists that could behave differently for CEM specifically.
14. **ISEB regression verified.** PASS — confirmed via the CSSE→ISEB switch test (item 9), which also exercised ISEB as the active pathway; top-bar switcher correctly showed "ISEB".
15. **Family Choice regression verified.** NOT VERIFIED interactively this round — no file under `lib/ali/persistence/familyFocusStore.ts` or the Family Choice route was touched; confirmed by code inspection only, not re-exercised live, due to time constraints.
16. **Mock history isolation verified.** NOT VERIFIED beyond code inspection — `lib/mockProgress.ts` was not modified this round; `MockResult.pathway` field confirmed unchanged in the client-side data model.
17. **Founder Validation isolation verified.** NOT VERIFIED interactively — no file under `app/learning-intelligence/founder-validation/*` or `lib/mockProgress.ts`'s `isFounderValidationResult()` was touched this round.
18. **Parent Dashboard verified.** PASS — `buildFocusAreas()` fix confirmed via code path (consumed by `/progress` and `/learning-intelligence/parent`'s shared, pathway-agnostic reporting); not separately screenshotted this round due to time constraints, but the underlying function change was directly unit-reasoned and matches the already-proven `buildDailyMission()` pattern.
19. **Mathematics Reference Vertical regression verified.** PASS — full lesson load (intro, worked examples, guided attempt) directly re-tested after this increment's changes; loads and renders correctly with no console errors, dash-punctuation-free (confirms the previous round's fix also still holds).
20. **Dynamic learner-facing copy checked for prohibited punctuation.** PASS — the confirmation dialog copy directly tested and confirmed dash-free; all new static copy in `PathwaySwitcher.tsx`, `PathwaySwitchConfirmDialog.tsx`, and the compact Beta page written without em/en dash from the outset.
21. **Production deployment verified against the actual deployed application.** See `PRODUCTION_DEPLOYMENT_REPORT.md`.
